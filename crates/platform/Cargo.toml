[package]
name = "nocaprs_platform"
description = "This defines the interfaces for a platform"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
tracing = "0.1.44"
ashpd = { version = "0.13.9", features = ["screenshot", "screencast"] }
